using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Projectile1 : Projectile
{
    public override void Init(float dmg, float speed, Entity target, Vector3 direction)
    {
        base.Init(dmg, speed, target, direction);
    }
}
